services:
  db:
    container_name: rei_do_bar-db
    image: postgres:18
    environment:
      - POSTGRES_DB=rei_do_bar
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=root
    ports:
      - "5432:5432"
    volumes:
      - ./.docker/postgres:/var/lib/postgresql:delegated