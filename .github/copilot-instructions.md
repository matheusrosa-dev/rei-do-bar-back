# Workspace Instructions

## AGENTS.md Convention

This project uses `AGENTS.md` files in each directory to document conventions, structure, and rules for that scope. **Before accessing or modifying any file in a directory, you must read the `AGENTS.md` file in that directory if one exists.**

These files are the source of truth for code conventions in their respective scopes. They take precedence over inferred patterns.