# Workspace Instructions

## CLAUDE.md Convention

This project uses `CLAUDE.md` files in each directory to document conventions, structure, and rules for that scope. **Before accessing or modifying any file in a directory, you must read the `CLAUDE.md` file in that directory if one exists.**

These files are the source of truth for code conventions in their respective scopes. They take precedence over inferred patterns.